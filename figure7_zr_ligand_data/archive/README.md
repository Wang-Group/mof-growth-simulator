# Figure 7 Archive

This archive keeps helper scripts that were useful during exploratory
post-processing but are not needed for the final lightweight `Figure 7` package.

## Current archived items

- `legacy_scripts/render_time_to_target_survival.py`
  Early standalone survival-plot helper that was superseded by the curated
  `make_figure7_draft.py` workflow and the local survival CSV in
  `zr_ligand_one_shot_minimal_summary/`.
