# Archive Notes

This archive keeps superseded exploratory files from the `Zr-link` one-shot
study so that the top-level summary folder remains focused on the final
manuscript-facing datasets.

## Current archived items

- `obsolete_mol_plots/`
  Early `BTB-MOL` dual-axis plots that were replaced by the final
  `11_mol_final_experiment_vs_kmc_logy.*` figure.
